import { j as m } from "../../../../../../../../../../_virtual/jsx-runtime.js";
import { useContext as u, Component as d } from "react";
import { usePresence as c } from "../../../components/AnimatePresence/use-presence.js";
import { LayoutGroupContext as f } from "../../../context/LayoutGroupContext.js";
import { SwitchLayoutGroupContext as l } from "../../../context/SwitchLayoutGroupContext.js";
import { globalProjectionState as h } from "../../../projection/node/state.js";
import { correctBorderRadius as n } from "../../../projection/styles/scale-border-radius.js";
import { correctBoxShadow as R } from "../../../projection/styles/scale-box-shadow.js";
import { addScaleCorrector as g } from "../../../projection/styles/scale-correction.js";
import { microtask as y } from "../../../frameloop/microtask.js";
import { frame as v } from "../../../frameloop/frame.js";
class T extends d {
  /**
   * This only mounts projection nodes for components that
   * need measuring, we might want to do it for all components
   * in order to incorporate transforms
   */
  componentDidMount() {
    const { visualElement: o, layoutGroup: e, switchLayoutGroup: t, layoutId: s } = this.props, { projection: r } = o;
    g(x), r && (e.group && e.group.add(r), t && t.register && s && t.register(r), r.root.didUpdate(), r.addEventListener("animationComplete", () => {
      this.safeToRemove();
    }), r.setOptions({
      ...r.options,
      onExitComplete: () => this.safeToRemove()
    })), h.hasEverUpdated = !0;
  }
  getSnapshotBeforeUpdate(o) {
    const { layoutDependency: e, visualElement: t, drag: s, isPresent: r } = this.props, i = t.projection;
    return i && (i.isPresent = r, s || o.layoutDependency !== e || e === void 0 ? i.willUpdate() : this.safeToRemove(), o.isPresent !== r && (r ? i.promote() : i.relegate() || v.postRender(() => {
      const p = i.getStack();
      (!p || !p.members.length) && this.safeToRemove();
    }))), null;
  }
  componentDidUpdate() {
    const { projection: o } = this.props.visualElement;
    o && (o.root.didUpdate(), y.postRender(() => {
      !o.currentAnimation && o.isLead() && this.safeToRemove();
    }));
  }
  componentWillUnmount() {
    const { visualElement: o, layoutGroup: e, switchLayoutGroup: t } = this.props, { projection: s } = o;
    s && (s.scheduleCheckAfterUnmount(), e && e.group && e.group.remove(s), t && t.deregister && t.deregister(s));
  }
  safeToRemove() {
    const { safeToRemove: o } = this.props;
    o && o();
  }
  render() {
    return null;
  }
}
function k(a) {
  const [o, e] = c(), t = u(f);
  return m.jsx(T, { ...a, layoutGroup: t, switchLayoutGroup: u(l), isPresent: o, safeToRemove: e });
}
const x = {
  borderRadius: {
    ...n,
    applyTo: [
      "borderTopLeftRadius",
      "borderTopRightRadius",
      "borderBottomLeftRadius",
      "borderBottomRightRadius"
    ]
  },
  borderTopLeftRadius: n,
  borderTopRightRadius: n,
  borderBottomLeftRadius: n,
  borderBottomRightRadius: n,
  boxShadow: R
};
export {
  k as MeasureLayout
};
