import { mapEasingToNativeEasing as p } from "../../../../../../motion-dom/dist/es/animation/waapi/utils/easing.js";
function y(t, e, o, { delay: s = 0, duration: n = 300, repeat: f = 0, repeatType: m = "loop", ease: l = "easeInOut", times: r } = {}) {
  const i = { [e]: o };
  r && (i.offset = r);
  const a = p(l, n);
  return Array.isArray(a) && (i.easing = a), t.animate(i, {
    delay: s,
    duration: n,
    easing: Array.isArray(a) ? "linear" : a,
    fill: "both",
    iterations: f + 1,
    direction: m === "reverse" ? "alternate" : "normal"
  });
}
export {
  y as startWaapiAnimation
};
