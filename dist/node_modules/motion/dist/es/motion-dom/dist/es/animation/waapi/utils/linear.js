import { progress as i } from "../../../../../../motion-utils/dist/es/progress.js";
const g = (e, o, s = 10) => {
  let t = "";
  const r = Math.max(Math.round(o / s), 2);
  for (let n = 0; n < r; n++)
    t += e(i(0, r - 1, n)) + ", ";
  return `linear(${t.substring(0, t.length - 2)})`;
};
export {
  g as generateLinearEasing
};
